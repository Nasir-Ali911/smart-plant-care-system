name: smart_plant_care
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  go_router: ^14.6.2

  # Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.4.1
  firebase_database: ^11.3.10
  cloud_firestore: ^5.6.1
  

  # Image selection
  image_picker: ^1.1.2
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true